-- Generated by tools/export_content.mjs. Do not edit by hand.
-- Source: data/spells/monster/seaSnakeFrostSpit.ts
local combat = Combat()
combat:setParameter(COMBAT_PARAM_TYPE, COMBAT_ICEDAMAGE)
combat:setParameter(COMBAT_PARAM_EFFECT, 4)

local condition = Condition(CONDITION_FREEZING)
condition:setParameter(CONDITION_PARAM_TICKS, 4000)
combat:addCondition(condition)

local speed = Condition(CONDITION_PARALYZE)
speed:setParameter(CONDITION_PARAM_TICKS, 4000)
speed:setParameter(CONDITION_PARAM_SPEED, -100)
combat:addCondition(speed)

local spell = Spell(SPELL_INSTANT)

function spell.onCastSpell(creature, variant)
  return combat:execute(creature, variant)
end

spell:name("Sea Snake Frost Spit")
spell:words("###generated_sea_snake_frost_spit")
spell:needTarget(true)
spell:range(4)
spell:blockWalls(true)
spell:needLearn(true)
spell:register()
